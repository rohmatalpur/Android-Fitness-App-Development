<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Glassmorphism Card Style -->
    <style name="GlassmorphismCard" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">8dp</item>
        <item name="cardBackgroundColor">@color/glass_white</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/glass_border</item>
    </style>

    <!-- Full Screen Video Style -->
    <style name="FullScreenVideoTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- Hero Animation Style -->
    <style name="HeroTransitionStyle">
        <item name="android:transitionName">hero_transition</item>
    </style>

</resources>
